# Session Report — 2026-03-18

## Summary
UI polish sprint. Synced git pull (722 insertions, 14 files), fixed 2 TS errors from the pull, then delivered 5 user-requested UI improvements plus end-to-end visual verification.

---

## Work Completed

### 1. TypeScript Fixes (from git pull)
- `AIStyleGenerator.tsx` + `StylesDrawer.tsx` — removed `id: crypto.randomUUID()` from `addCustomStyle()` calls
- Root cause: `addCustomStyle` signature changed to `Omit<CustomStyle, 'id'>` in the pull
- Result: 0 TS errors maintained

### 2. Nav Reorder + Mobile Bottom Nav Redesign
**File:** `apps/web/src/components/layout/TopNav.tsx`

Desktop NAV_ITEMS order: Builder → Moodboard → Generate → Library

Mobile bottom nav:
- Builder tab gets `primary: true` → elevated floating circle (`-mt-5 w-14 h-14 rounded-full`, `bg-accent` active, `shadow-[0_0_20px_rgba(255,107,0,0.45)]`)
- Regular tabs: `w-8 h-8 rounded-xl` tinted ring style
- Container uses `items-end` so the elevated button floats above

### 3. TemplateCard 2-Column Slideshow Layout
**File:** `apps/web/src/components/builder/TemplateCard.tsx`
**New file:** `apps/web/src/lib/data/template-showcase.ts`

- Desktop (sm+): `flex-row h-[140px]` — left 55% metadata, right 42% visual panel
- Right panel: gradient placeholder (`bg-gradient-to-br from-bg-3 via-bg-2 to-bg-base` + accent tint overlay + label)
- When images provided in `TEMPLATE_SHOWCASE[template.id]`: crossfade slideshow (`transition-opacity duration-700`), `onError` handler + `erroredSrcs` Set to skip broken images, active dots indicator
- Left-edge gradient: `bg-gradient-to-r from-bg-2 to-transparent` blends columns
- All 21 template IDs mapped in `template-showcase.ts`, all arrays currently empty (safe — no Image components rendered, no 400 errors)

### 4. Builder Step Nav Redesign
**File:** `apps/web/src/app/(app)/builder/page.tsx`

- 6 `WORKFLOW_STEPS`: `[{id:'fields', Icon:FileText}, {id:'styles', Icon:Paintbrush}, {id:'colors', Icon:Palette}, {id:'keywords', Icon:Tags}, {id:'negative', Icon:Ban}, {id:'mockup', Icon:Layers}]`
- Step dots: `w-7 h-7 rounded-full` — active = accent bg + glow, completed = accent/15, future = glass
- Prev button: hidden on first step; shows `<PrevIcon /> <PrevLabel>` with `min-w-[90px]`
- Next button: shows `<NextLabel> <NextIcon>` or "Done ✓" (with `Check` icon) on last step

### 5. Pricing Page + UserMenu Link
**New file:** `apps/web/src/app/(public)/pricing/page.tsx`
**Modified:** `apps/web/src/components/layout/TopNav.tsx`

Pricing page features:
- Monthly/Yearly toggle: Pro $12/mo or $99/yr ($8.25/mo, saves 31%)
- Free card: `bg-bg-2 border-glass-border`, "Get Started Free" → `/login`
- Pro card: `border-accent/30 shadow-[0_0_40px_rgba(255,107,0,0.08)]`, "MOST POPULAR" badge, orange CTA → `/login?plan=pro`
- 9-row feature comparison table with striped rows
- FAQ accordion (3 items, `useState` per item)
- Footer email: `hello@savageprompt.com`

UserMenu: Pricing link (Sparkles icon) added above Settings item in dropdown.

---

## Visual Verification (Playwright)

| Page | Status | Notes |
|------|--------|-------|
| `/pricing` | ✅ | Monthly/Yearly toggle, Free $0 + Pro $12 "MOST POPULAR", orange CTA, table, FAQ |
| `/builder` (picker) | ✅ | 2-column template cards, clean gradient right side, Builder nav first |
| `/builder` (Clothing) | ✅ | 6 icon-circle dots, Fields active (orange glow), "Styles ›" named next button |

---

## TypeScript
```
npx tsc --noEmit  →  0 errors
```

---

## Deferred / Next Up

| Item | Reason deferred |
|------|----------------|
| Stripe integration | Need Stripe price IDs; `/login?plan=pro` is placeholder CTA for now |
| Magic UI install | Phase 1 redesign task — still not started |
| Showcase image QA | FLUX.1-schnell has mediocre prompt adherence — may need `fal-ai/flux/dev` |

---

## Files Changed (Session 1 — UI Polish)

| File | Change |
|------|--------|
| `apps/web/src/components/layout/TopNav.tsx` | Nav reorder + mobile redesign + pricing link |
| `apps/web/src/components/builder/TemplateCard.tsx` | 2-col layout + gradient + slideshow |
| `apps/web/src/app/(app)/builder/page.tsx` | Step nav redesign |
| `apps/web/src/app/(public)/pricing/page.tsx` | NEW — full pricing page |
| `apps/web/src/lib/data/template-showcase.ts` | NEW — empty template image path map |
| `apps/web/src/components/builder/AIStyleGenerator.tsx` | TS fix |
| `apps/web/src/components/builder/StylesDrawer.tsx` | TS fix |

---

# Session 2 — Template Showcase Images (Evening)

## Summary
Generated all 42 AI showcase images for the 21 template cards using FAL.ai FLUX.1-schnell. Extended the generation script with `--fal` mode after exhausting credits on 3 other providers. All images committed and pushed.

### At a Glance
- **42/42 images generated** via FAL.ai FLUX.1-schnell — 0 failures
- **4 AI providers tried**: Pollinations (8/42, credits out) → HF FLUX.1-schnell (42 but wrong content) → HF SDXL (14/42, credits out) → Together.ai (no credits) → FAL.ai ✅
- **Script supports 4 modes**: default (loremflickr), `--ai` (Pollinations), `--hf` (HF SDXL), `--fal` (FAL.ai)

## Work Completed

### FAL.ai Integration (`--fal` mode)
Added `falGenerate()` to `scripts/generate-template-showcase.ts`:
- POST to `https://fal.run/fal-ai/flux/schnell` with `Key {id}:{secret}` auth
- JSON response → fetch image URL → save as JPG
- 3-retry logic with exponential backoff for 429/500
- 2s delay between requests

### 42 Template Showcase Images
All 21 templates × 2 images each, saved to `public/showcase/templates/{id}-{1,2}.jpg` (21KB–660KB).

### Data File Regenerated
`template-showcase.ts` auto-updated with all 42 image paths across 21 template IDs.

### Pricing page route move
Moved from `(public)/pricing` to `(app)/pricing` route group.

## Files Changed (Session 2)

| File | Change |
|------|--------|
| `apps/web/scripts/generate-template-showcase.ts` | Added `--fal` mode with `falGenerate()` |
| `apps/web/src/lib/data/template-showcase.ts` | Regenerated with all 42 image paths |
| `apps/web/public/showcase/templates/*.jpg` | 42 new AI-generated images |
| `apps/web/src/app/(app)/pricing/page.tsx` | Moved from `(public)` route |
| `apps/web/src/components/layout/TopNav.tsx` | Minor updates |
| `apps/web/src/components/ui/AnimatedLayout.tsx` | Minor updates |
| `apps/web/src/app/(app)/layout.tsx` | Minor updates |

## Git
- Commit: `7cee2f2` — `feat(showcase): add AI-generated template showcase images via FAL.ai`
- Pushed to `master`

## TypeScript: 0 errors
